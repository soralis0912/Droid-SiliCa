You are working inside Droid-SilliCa, a Kotlin Android app (minSdk 24, ViewBinding, Material Design, ViewPager2 tabs). Architecture follows a lightweight MVC: TabController aggregates Read/Write/Manual/History controllers and exposes immutable TabContent models; TabFragment picks the appropriate TabView implementation (ReadView/WriteView/etc.) to render `FragmentTabBinding`; BaseTabView handles shared title/description/action list rendering. Key sources live under `app/src/main/java/org/soralis/droidsillica` (`ui/MainActivity`, `ui/tab/TabFragment` & `TabPagerAdapter`, `controller/TabController` + `controller/tab/*`, `model/TabContent`, `ui/tab/view/*`). Keep tab data inside controllers and only do view logic in TabView subclasses. Use the Kotlin + AndroidX dependencies already configured in `app/build.gradle.kts`. Build with `./gradlew assembleDebug`.
